const container = `w-80 border-2 border-gray-500 rounded-lg shadow-lg p-4 flex flex-col gap-4 bg-gray-100 hover:bg-gray-200 transition duration-180 ease-in-out transform hover:-translate-y-s1 hover:scale-105 cursor-pointer`;
const displayName = `text-2xl font-bold text-center pb-2`;
const image = `w-40 h-40 mx-auto rounded-full`;
const description = `py-2 text-center`;

export { container, displayName, image, description };
